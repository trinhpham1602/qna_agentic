Todo:
I. dataset
    1. Thêm mới một số intent thuộc về nghiệp vụ đặt vé máy bay. Cập nhật vào file intents.json
    2. entities.json chỉ chứa các đối tượng cụ thể. không được chứa các đối tượng chung chung. ví dụ như id=hanh_khach.
        Các đối tượng cụ thể được gom nhóm, bởi field type như hiện tại.
        THÊM các entity đầy đủ cho nghiệp vụ máy bay. 
    3. claude_dataset chỉ chứa atomic entity, KHÔNG ĐƯỢC chứa các type chung chung.
II. Phần slot mapping:
    1. Nếu atomic thuộc type required của slotmapping thì yêu cầu user nhập thêm vào
III. uitls.py
    1. tôi muốn output của model ML trích xuất được multi intents và multi
    2. thêm knowledge gragh traverse vào để tôi có thể test path

Nếu dùng lib thì thêm vào file requirements.txt để cài mới và tôi cũng có .venv cho project này.
