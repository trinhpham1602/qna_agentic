                ┌──────────────┐
User Query ───► │ Normalizer   │ (expand abbrev)
                └──────┬───────┘
                       ↓
                ┌──────────────┐
                │ Query Expander│ (LLM multi-query)
                └──────┬───────┘
                       ↓
         ┌─────────────┴─────────────┐
         ↓                           ↓
   Vector Search                BM25 Search
         ↓                           ↓
         └──────────┬───────────────┘
                    ↓
               Reranker
                    ↓
                 LLM Answer


model: qwen2.5
embedding: intfloat/multilingual-e5-large
fuzzy matching + normalization layer, tại bước nomalizer

from rapidfuzz import fuzz

def match_abbr(token):
    for key in ABBREVIATIONS:
        score = fuzz.ratio(token, key)
        if score > 80:
            return key


token
  ↓
Exact match (fast path)
  ↓
Fuzzy match
  ↓
Embedding match (fallback)


👉 implement nhanh:

fuzzy match (rapidfuzz)
multi-candidate expansion
hybrid search

👉 là đã đủ mạnh cho 80% case

Mô hình lựa chon V1
Input query
   ↓
Tokenize
   ↓
Detect abbreviation (exact → fuzzy → semantic optional)
   ↓
Resolve → candidates
   ↓
Generate expanded queries
   ↓
Return multi-query list

ex:
{
  "content": "Eco có hành lý xách tay 7kg",
  "metadata": {
    "airline": "vietjet",
    "policy_type": "baggage",
    "fare_class": "eco"
  }
}

Notes
B1: chinh cau query cho dung => normalized query
B2: chon decomposition query => chia thanh sub queries

các bước để tạo một data set:

Tôi có nhiều entity và tôi ví dụ ở đây là hành lý:
Hành lý có các tình huống cho hành lý, tình huống sẽ cụ thể cho từng hạng vé, và có độ risk riêng cho tình huống, nếu risk cao thì sẽ gọi cho nhân viên (chẳn hạn)

Giúp tôi tổ chức một data set trả lời cho câu hỏi dạng multi query 


"canonical": ["baggage", "refund", "reissue", "policies", "sepecial_service_request"]